models:
 - name: stg_spotify__tracks
   description: "Staging model that cleans and standardizes raw Spotify track data."
   columns:
    - name: track_id
      description: "Unique identifier for each track."
      data_tests:
       - not_null
       - unique